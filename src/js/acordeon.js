import Accordion from 'accordion-js';

document.addEventListener('DOMContentLoaded', function () {
  // Инициализация аккордеона
  const accordionAbout = new Accordion (' .accordion-about-container');
  const accordionFaq = new Accordion(' .accordion-faq-container');
  // const button = document.querySelector('.arrow-btn');
  // // Обработчик события для кнопки разворачивания аккордеона
  // button.addEventListener('click', function () {
  //   accordion.toggleAll();
  // });
});
